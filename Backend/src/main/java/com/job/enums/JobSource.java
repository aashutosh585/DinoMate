package com.job.enums;

public enum JobSource {
    PORTAL,       // Direct job posted on DinoMate portal (seekers apply with resume & screening questions)
    AGGREGATED    // External job aggregated from 150+ platforms (Greenhouse, Lever, Ashby, LinkedIn, etc.)
}
